<!--  app/views/movies/show.html.erb -->
<h2>Details about <em><%= @task.title %></em></h2>

<ul id="details">
  <li>
    Created on:
    <%= @task.created_at.strftime("%B %d, %Y") %>
  </li>

  <li>
    Duration:
    <%= @task.duration %>
  </li>
</ul>

<h3>Description:</h3>
<p id="description">
  <%= @task.description %>
</p>

<div class="row">
  <%= link_to 'Edit', edit_task_path(@task), class: 'btn btn-primary col-2' %>
  <%= link_to 'Delete', task_path(@task), 'data-method': 'delete', 'data-confirm': 'Are you sure?', class: 'btn btn-danger col-2' %>
  <%= link_to 'Back to tasks list', tasks_path, class: 'btn btn-primary col-2' %>
</div>
